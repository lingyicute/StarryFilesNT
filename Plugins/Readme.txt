This directory contains sample plugins that demonstrate functionality offered by the StarryFiles++ plugin API. Currently, the plugin system is in development.

To load the plugins here, go through the following steps:

1. Copy the desired plugins into a "plugins" folder in the same directory as the StarryFiles++ executable.
2. Start StarryFiles++ with plugin support enabled:

explorer++.exe --enable-features Plugins